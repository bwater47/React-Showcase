// Import the ReactDOM library from the react-dom package
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./index.css";
// Use the createRoot method from ReactDOM to render the App component to the root element
ReactDOM.createRoot(document.getElementById("root")).render(<App />);
