// Export the resume function for the app to display the resume page when the user clicks on the resume tab.
export default function Resume() {
  return (
    <div>
      <a href="drive link">Resume</a>
    </div>
  );
}
